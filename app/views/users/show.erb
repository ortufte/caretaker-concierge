<h2> Welcome to your dashboard, <%= @user.name %>! </h2>

<h3>Your dependents and their activities:</h3>
<br>
<% @user.dependents.each.with_index(1) do |d, i| %>
    <h4> <%= i %>. <a href="/dependents/<%=d.id%>"><%= d.name %></a> </h4>
    <br>
    <% d.activities.each do |a| %> 
       <p><a href="/activities/<%= a.id %>"><%= a.title %></a></p>
    <% end %>
    <br>
    <% end %>

<h5>Click on the name of the dependent or activity to view more details.</h5>
<br>
<h5>OR</h5>
<br>
<a href="/dependents/new">Create a New Dependent</a>
<br>
<a href="/activities/new">Create a New Activity</a>
<br>
<a href="/users/logout">Logout</a>
